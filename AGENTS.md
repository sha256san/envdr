# AI エージェント開発ルール (AGENTS.md)

このドキュメントは、AI エージェントが `envdoctor` プロジェクト内でコード変更・リファクタリング・機能追加・ドキュメント更新を行う際の行動規範とガイドラインを定めます。

---

## 1. 基本原則 (Core Principles)

1. **ドキュメントとコードの整合性維持**
   - 新機能や仕様変更を行う際は、必ず `SPEC.md`（仕様書）と `TODO.md`（タスク一覧）を更新すること。
   - 設計判断や知見が得られた場合は `MEMORY.md` に追記すること。
   - 変更履歴は `CHANGELOG.md` に記録すること。

2. **安全第一・非破壊設計**
   - ユーザーの環境設定を破壊したり、不可逆な変更を勝手に行うコマンドをコードやテストに含めないこと。
   - システム診断処理は、常にリードオンリー（副作用なし）で動作すること。

3. **高い可読性とエラーハンドリング**
   - `unwrap()` や `expect()` の安易な使用を避け、`Result` や `Option` を適切にハンドリングすること。
   - OS固有のコマンド実行失敗（コマンドが存在しない等）は正常な診断結果（`Status::Warning` または `Status::Error`）として扱い、CLI 自体がパニックしないこと。

---

## 2. Rust コーディング規約 (Rust Style Guidelines)

- **モジュール分割**:
  - `src/core/`: ドメインモデル、ステータス、共通トレイト
  - `src/system/`: OS情報、環境変数、システムリソース
  - `src/checkers/`: 各ツール・言語・ランタイム診断ロジック
  - `src/output/`: ターミナル、JSON、Markdown のフォーマッター
  - `src/utils/`: 汎用ヘルパー（コマンド実行、パス正規化、サニタイズ）
- **トレイト指向**:
  - すべての診断モジュールは `core::Checker` トレイトを実装する。
- **依存クレートの最小化**:
  - 不要に巨大な依存関係を追加せず、標準ライブラリで容易に実現できるものは自前実装を優先する。
- **テストの徹底**:
  - 単体テスト（Unit test）をモジュール内に記述し、CI で自動検証可能に保つ。

---

## 3. ドキュメント更新フロー (Documentation Workflow)

```text
1. ユーザー要望 / 要件確認
   ↓
2. SPEC.md / TODO.md の更新
   ↓
3. 実装 (src/ 配下) & テスト (cargo test)
   ↓
4. MEMORY.md に学び・設計決定を記録
   ↓
5. CHANGELOG.md に変更差分を記録
```

---

## 4. プロジェクト階層規約

```text
project/
├── SPEC.md        # 要件・仕様書
├── TODO.md        # 未実装一覧・タスク
├── MEMORY.md      # AIが学習した知見・設計判断
├── AGENTS.md      # AIへのルール（本文書）
├── CHANGELOG.md   # 変更履歴
├── Cargo.toml     # ビルド設定
└── src/           # ソースコード
```
このファイル階層構造を常に維持し、勝手なルートディレクトリの乱雑化を防ぐこと。
